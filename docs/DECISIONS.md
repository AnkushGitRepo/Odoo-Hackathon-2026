# Decision Log

Append-only. Newest at the bottom. Decisions here are settled — don't re-litigate; add a new
entry if one must be reversed.

| ID | Date | Decision | Why |
|---|---|---|---|
| D-001 | 2026-07-12 | Next.js 15 monolith (App Router), no separate backend | One repo/process for 2-dev 8h team; server actions remove API boilerplate |
| D-002 | 2026-07-12 | MongoDB Atlas via Prisma (team choice, replaced SQLite proposal) | Team preference; Atlas gives replica set (required by Prisma Mongo connector for transactions) with zero setup |
| D-003 | 2026-07-12 | Role stored as enum on User; no separate Roles collection | 4 fixed roles; a join collection adds cost with no benefit in 8h. "Roles" entity from brief is satisfied by the enum |
| D-004 | 2026-07-12 | All business rules as pure functions in `src/lib/rules.ts` | Single testable source; server actions + UI share it; judges test rules hardest |
| D-005 | 2026-07-12 | Server actions (not REST API routes) for all mutations | Less code, typed end-to-end; nothing external consumes an API |
| D-006 | 2026-07-12 | Maintenance cost read from MaintenanceLog, never copied into Expense | Avoids double counting in operational cost |
| D-007 | 2026-07-12 | Login does NOT let the user pick a role (mockup shows a role dropdown) | Selectable role defeats RBAC; role comes from the seeded account. Judges get 4 demo logins instead |
| D-008 | 2026-07-12 | Work directly on `main`, hourly pushes, module ownership in docs/TASKS.md | Hackathon requires latest code on main + per-member commits; ownership prevents conflicts |
| D-009 | 2026-07-12 | Trip codes TR001… generated from a counter query at creation | Human-readable ids matching mockup; Mongo ObjectIds stay internal |
| D-010 | 2026-07-12 | **Reverses D-001/D-005**: MERN stack — React SPA (Vite) + Express REST API monorepo, not Next.js | Team choice (Ankush). Client and server split cleanly between two devs; npm workspaces keep one repo |
| D-011 | 2026-07-12 | **Reverses D-002 (partially)**: Mongoose ODM instead of Prisma; MongoDB stays | Team wanted plain MongoDB without Prisma; Mongoose confirmed over native driver for schema validation + unique indexes |
| D-012 | 2026-07-12 | Auth = JWT (jsonwebtoken) + bcryptjs, `Authorization: Bearer` header; Auth.js dropped with Next.js | Standard MERN pattern; requireAuth/requireRole middleware enforce RBAC server-side |
| D-013 | 2026-07-12 | API envelope `{ success, data?, error?, code?, details? }` on every endpoint | Consistent client handling; `code` is machine-stable, `error` is display-ready |
| D-014 | 2026-07-12 | **Backend-first build order**; the API contract in AGENTS.md is binding — backend implements it exactly, frontend codes against it without guessing; contract changes go to AGENTS.md + this log BEFORE code | Two agents (Claude Code + Antigravity) must stay in sync without real-time coordination |
| D-015 | 2026-07-12 | Tasks in docs/TASKS.md are unowned; claim by marking `doing` + your name and pushing the doc change | Team preference — flexible pickup over fixed ownership |
| D-016 | 2026-07-12 | RBAC: writes strictly scoped per mockup matrix, but reads broadly shared (every role gets "view" on most modules) | Screens need vehicle/driver names to render; write-scoping is what the rules require. Analytics stays hidden from Dispatcher + Safety Officer |
| D-017 | 2026-07-12 | Vehicle DELETE = soft retire (status `RETIRED`), documents never deleted | Preserves trip/expense history and ROI math |
| D-018 | 2026-07-12 | Trip completion auto-creates a FuelLog from `fuelUsedL` + `fuelCost` | Matches mockup flow "On Complete: odometer → fuel log → expenses"; keeps fuel efficiency computable without double entry |
| D-019 | 2026-07-12 | **Refines D-014**: page-by-page vertical slices (landing → auth → dashboard → one module page at a time), each slice = its backend routes + UI; API contract stays binding | Team decision; lets Ankush + Dev Panchal work in parallel with visible progress every hour |
| D-020 | 2026-07-12 | Public signup (`POST /auth/register`) with role selection at account creation; login stays role-free (D-007 upheld) | Judges need to self-serve any role in the demo. Production would use invite/admin-assigned roles — noted simplification |
| D-021 | 2026-07-12 | Visual language locked from reference images (`design/reference-landing.png`, `design/reference-auth.png`): mist ice-blue canvas, slate ink, royal-indigo accent, floating rounded-3xl cards, pill CTAs; pages built with impeccable + design-taste skills, GSAP motion | Team-chosen style; specs in DESIGN.md |
| D-022 | 2026-07-12 | **Refines D-015**: Phase-1 tasks pre-assigned between Ankush and Dev Panchal in docs/TASKS.md; later phases assigned at phase start | Team asked for an explicit division of work |
| D-023 | 2026-07-12 | API port moved 5000 → 5001 (server default, Vite proxy, all docs) | macOS AirPlay Receiver (ControlCenter) occupies 5000 on dev machines |
| D-024 | 2026-07-12 | T4 (auth pages) and T9 (basic dashboard) built by Ankush instead of Dev — Ankush directed the change mid-phase; Dev's remaining Phase-1 tasks are T5 (models), T6 (seed), T7 (KPI endpoint) | Kept momentum; auth pages needed the T2/T3 plumbing that was already in Ankush's context |
| D-025 | 2026-07-12 | Fixed a real bug in Dev's T7 (`server/src/routes/dashboard.ts`): the catch block did `throw error` instead of `next(error)` | Express 4 does not auto-forward async-handler rejections; a rethrow there produced an unhandled promise rejection and the request just hung with no response. Also tightened `Record<string, any>` filter object to a typed `VehicleFilter` interface (project bans `any`) |
| D-026 | 2026-07-12 | **Refines the Driver entity**: `licenseCategory` is `LicenseCategory[]` (non-empty array), not a single value | A driving license can legitimately cover both LMV and HMV classes at once; a single-value field couldn't represent that. Updated Driver.ts, seed.ts (Priya and Ramesh now hold both), client types.ts, AGENTS.md contract, ARCHITECTURE.md |
| D-027 | 2026-07-12 | Phase 2 split: Ankush takes M1 (Fleet), M3 (Trips + rules engine), M7 (dashboard finishers), M8 (Settings); Dev takes M2 (Drivers), M4 (Maintenance), M5 (Fuel & Expenses), M6 (Analytics + CSV) | All four of Dev's slices are buildable directly against the Mongoose models he already shipped in T5, so neither person blocks the other; only soft dependency is Dev's vehicle-picker forms wanting `GET /api/vehicles` from Ankush's M1. Detailed task prompt: docs/PROMPT_DEV_PHASE2.md |
| D-028 | 2026-07-12 | M3's Trip Dispatcher UI calls `GET /api/drivers/assignable` (Dev's M2, not yet built) but degrades gracefully — catches the 404, disables the driver select, shows "Driver list isn't available yet" — rather than building a stub `drivers.ts` route file | Building even a partial `drivers.ts` risks a real merge conflict when Dev pushes his own M2 file with the same path; graceful degradation against the documented contract means the picker starts working automatically the moment Dev's route lands, no code change needed |
| D-029 | 2026-07-12 | Reviewed and merged Dev's M2/M4/M5/M6; fixed one real bug in `analytics.ts`'s CSV export (`.join("\\n")` produced literal backslash-n bytes, not real newlines — confirmed via raw byte inspection, every exported row would have landed in one spreadsheet cell). Also fixed a wiring gap: `App.tsx` still imported Drivers/Maintenance/Expenses/Analytics from the placeholder stubs in `modules.tsx` instead of Dev's real page files, so all four pages were silently dead code until this fix. Retyped `Record<string, any>` to `Record<string, unknown>` in drivers.ts/maintenance.ts/fuelLogs.ts/expenses.ts (project bans `any`) | Both defects were invisible without live testing — the CSV bug only shows up inspecting raw bytes, and the routing gap only shows up navigating to the pages, not from reading the diff |
| D-030 | 2026-07-12 | M7 dashboard region filter is populated by fetching all vehicles and deriving distinct `region` values client-side, rather than a dedicated `/api/regions` endpoint | `region` is free-text in the contract, not an enum; no endpoint was specified for a distinct-values list, and adding one would be undocumented scope creep. Deriving client-side reuses the existing `/api/vehicles` endpoint |
| D-031 | 2026-07-12 | M8 Settings general form (depot name, currency, distance unit) persists to `localStorage` only, no backend endpoint | The AGENTS.md contract does not define a settings endpoint; adding one would be scope creep beyond the documented API surface. The RBAC matrix half of the page is genuinely read-only, sourced directly from `lib/rbac.ts` |
| D-032 | 2026-07-12 | Deleted `client/src/pages/modules.tsx` and `client/src/components/PlaceholderPage.tsx` once M8 (the last module) shipped | Both were scaffolding for slices that no longer exist as placeholders; keeping unused files around after all M1-M8 slices landed was dead code |
| D-033 | 2026-07-12 | Phase 3 (final polish) split: Ankush takes N1 (fix `/api/trips` 404 regression, discovered mid dark-mode testing), N2 (dark mode infra + his own pages, done), N4 (responsive pass, his pages), N6 (E2E smoke test), N7 (fresh-clone test + README); Dev takes N3 (dark mode, his 4 pages) and N5 (responsive, his 4 pages), with N8 (license-expiry banner) as optional stretch | Same file-ownership split as Phase 2 avoids conflicts; dark-mode convention is fully documented with copy-paste patterns pulled from Ankush's already-shipped code so Dev needs zero back-and-forth. Detailed prompts: docs/PROMPT_ANKUSH_PHASE3.md, docs/PROMPT_DEV_PHASE3.md |
| D-034 | 2026-07-12 | **Fixed**: `/api/trips` 404 regression — `tripsRouter` import + mount were missing from `server/src/index.ts`. Root cause confirmed via `git show 6791dcd` (the merge commit, authored by Dev): his branch was based on `7224d9f` (before M3/trips existed), so his `index.ts` already had 6 new router imports by the time he merged Ankush's `main` in; git's 3-way merge combined the two independently-inserted import blocks but the `trips` lines were silently dropped despite no conflict being reported. `server/src/routes/trips.ts` itself was never touched — only the mount. Re-added both lines, re-ran the full 15-case curl regression suite against Atlas — all pass — and verified in-browser | Added a rule to AGENTS.md: after any merge touching `server/src/index.ts`, diff it and confirm every router is still mounted, since git's line-based merge doesn't guarantee both sides' independent insertions survive |
| D-035 | 2026-07-12 | Found (assigned to Dev as part of N3): `text-mist-500` and `text-mist-800` are used across DriversPage/MaintenancePage/ExpensesPage/AnalyticsPage but neither color stop is defined in the `mist` `@theme` scale (only 50/100/200/300 exist) — the classes silently no-op | Discovered while writing the Phase 3 dark-mode convention table and cross-checking Dev's files against the actual theme tokens; bundled into his dark-mode pass since he's already touching those exact lines |
| D-036 | 2026-07-12 | N4 (responsive pass) fixed 4 real gaps: Dashboard's Recent Trips table lacked a horizontal-scroll wrapper; VehicleForm/TripForm/CompleteTripDialog used bare `grid-cols-2` (not responsive) for their field grids, staying 2-column and cramped even at 375px | Found by testing every owned page at 375px and 768px with agent-browser rather than assuming the mobile-first Tailwind classes elsewhere in the codebase meant every grid was already responsive |
| D-037 | 2026-07-12 | Live deployment (client + API to external hosts) explicitly deferred to a final Phase 6, run only after everything else is settled | Ankush's request. The hackathon brief only mandates a public GitHub repo; a live URL is a bonus, not a requirement, and touches external accounts/credentials that shouldn't be assumed mid-plan |
| D-038 | 2026-07-12 | Phase 5 split: Ankush takes S1 (security hardening: rate limiting on auth, secrets audit, CORS/error-leakage review), S2 (adversarial QA on his own pages), S3 (docs/submission checklist); Dev takes S4 (adversarial QA on his 4 pages), S5 (security self-check on his routes), with S6 (PDF export via browser print, not a new server dependency) as optional stretch | Same file-ownership split as Phases 2-3. Detailed prompts: docs/PROMPT_ANKUSH_PHASE5.md, docs/PROMPT_DEV_PHASE5.md |
| D-039 | 2026-07-12 | S1 done: added `express-rate-limit` to `POST /api/auth/login` and `POST /api/auth/register` only (20 req / 15 min / IP), new `RATE_LIMITED` 429 error code documented in AGENTS.md | Guards the two credential-handling endpoints against brute-force/credential-stuffing without limiting the read-heavy rest of the API, which would risk breaking demo usage for judges clicking around |
| D-040 | 2026-07-12 | S3: untracked `.claude/settings.local.json` (added to `.gitignore`, `git rm --cached`, local file preserved) — it was accidentally committed in the very first Phase 0 commit, before the convention of excluding it was established | It's per-developer AI-tool config, not project code; leaving it tracked risked Dev's own local settings getting clobbered on pull |
| D-041 | 2026-07-12 | S3: found but deliberately **not** removed — `.claude/skills/` and `.agents/skills/` (the full vendored `impeccable`/`gsap-*`/`design-taste-frontend` skill packages, hundreds of files) are committed to the repo | Flagged to Ankush rather than acted on unilaterally: `git rm --cached` only preserves files on the machine that runs it — everyone else's next `git pull` actually deletes the files from their disk, which could break Dev's local Antigravity skill setup mid-hackathon. Cleanliness gain is low-stakes for judging; breakage risk to a teammate's environment this late is not worth it without an explicit decision |
| D-042 | 2026-07-12 | S4 (adversarial QA) fixed a real client-side RBAC gap: DISPATCHER and SAFETY_OFFICER could navigate to `/analytics` and `/expenses` by typing the URL directly, bypassing the sidebar's `null`-access check | `ProtectedRoute` only checks authentication (`!user`), not per-module authorization. Added `can(user!.role, module) === null` checks directly inside `AnalyticsPage` and `ExpensesPage` that redirect to `/dashboard`, enforcing the matrix even on direct navigation |
| D-043 | 2026-07-12 | Phase 6 (deployment) architecture: two separate Vercel projects — `transitops-api` (Express as a serverless function) and `transitops-client` (static Vite build) — same Vercel team Ankush already has working access to, no combined single-project static+function build | Confirmed via the Vercel MCP tool: the account has no existing TransitOps project, and already runs other apps as separate `*-client`/`*-api` project pairs, so this follows an established, lower-risk pattern rather than inventing a riskier combined build this late |
| D-044 | 2026-07-12 | Server keeps its existing `npm run dev` / `app.listen()` entry point completely untouched; a new `server/api/index.ts` (Vercel's serverless convention) imports the same app-construction logic and adds a cached Mongoose connection promise for reuse across warm-container invocations | Avoids any risk to the local dev workflow both Ankush and Dev depend on; serverless + Mongoose requires connection caching or every request opens a fresh DB connection, exhausting Atlas's connection limit under load |
| D-045 | 2026-07-12 | Client's API base URL becomes `import.meta.env.VITE_API_URL ?? "/api"` instead of the hardcoded `"/api"` | Relative `/api` only works when client and API share an origin (true locally via the Vite dev proxy, false once they're on separate Vercel domains); the env var is unset locally so the fallback preserves the existing dev workflow exactly |
| D-046 | 2026-07-12 | Phase 6 split: Ankush owns the full deployment chain (V1-V7) end to end rather than splitting by file ownership like prior phases; Dev's V8/V9 are live-environment verification + submission wrap-up, explicitly gated on Ankush's deployment being done first | Deployment is one continuous, hard-to-reverse sequence (restructure → deploy API → deploy client → close CORS loop → verify), not naturally parallel work; Ankush pauses for explicit confirmation before setting real production secrets and before both `target: "production"` deploy calls |
| D-047 | 2026-07-12 | Ankush directed removal of 5 mockup-derived UI elements before Phase 6: Settings' RBAC matrix card, Trips' lifecycle-stepper banner (and its now-dead `LifecycleStepper.tsx`), Drivers' footer warning note, Fleet's footer rule note, Maintenance's footer note | These were explanatory copy carried over from the Excalidraw mockup, not functional requirements — the underlying RBAC/rules enforcement is untouched (still enforced server-side and via `can()`), only the redundant on-page text/table describing those rules was removed for a cleaner interface |
